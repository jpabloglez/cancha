/**
 * Next.js configuration.
 *
 * Strict React mode is enabled; the public API base URL is exposed to the
 * client via NEXT_PUBLIC_API_BASE_URL (see .env.local).
 */

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
